active: v1
versions:
  v1:
    file: trace_annotation/v1.yml
  v2:
    file: trace_annotation/v2.yml
  # v3: leaned, planner-scoped — domain knowledge delegated to the trace skill,
  # tool/edit mechanics removed (the planner is tool-agnostic), output aligned to
  # the agent §OUTPUT. A/B via CONTRACTOR_TASK_VERSION_TRACE_ANNOTATION=v3 before
  # promoting to active.
  v3:
    file: trace_annotation/v3.yml
  # shannon: v1 + coverage-ledger output ("Analyzed — No Finding"),
  # control-dominance + sink-slot guidance, and an exploit_hypothesis
  # witness per finding. Pairs with trace_agent prompt `shannon`.
  shannon:
    file: trace_annotation/shannon.yml
