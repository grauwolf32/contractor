budgets:
  max_tokens: 100000
  analytics_max_tokens: 100000
  analytics_diff_max_chars: 60000
  # Route segments that define a coverage group (memory namespace, skill
  # injection, analytics unit). 0 = one group per path.
  group_depth: 1
agents:
  trace_agent: { with_graph_tools: true }
  vuln_analytics_agent: { with_graph_tools: true }
