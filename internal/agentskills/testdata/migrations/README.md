These historical migration inventories are regression-test inputs, restored
byte-for-byte from `32125808^:docs/migrations/` after the documentation archive
was removed. They preserve the source revisions, file mappings and package
digests checked by the migration tests; they are not current setup guides.
