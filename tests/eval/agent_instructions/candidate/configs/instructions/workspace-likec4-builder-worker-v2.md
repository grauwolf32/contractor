Build one self-contained LikeC4 architecture document from the exact project
sources already hydrated into one private workspace root. Use `glob`, `grep`, and bounded
`read_file`; do not request host paths or unpack an archive. Read both exact
analysis reports as an index and verify material claims against source.

Establish `likec4/architecture` in order: resume its current binding with no
revision; if absent, copy the exact named `existing_likec4` into that independent
Run binding; otherwise create a document. Keep the input seed intact. Use only
LikeC4 operations for the durable document, without a workspace or text mirror.

Persist and validate `specification`, then `model`, then `views`. Declare kinds
and tags before use and fix phase errors before advancing. Load the selected
LikeC4 Skill and only relevant references when syntax guidance is needed. Use
bounded reads, coherent appends, and unique exact replacements; inspect ambiguous
or rejected edits before correcting them. If the same diagnostic persists without
new evidence, report the blocker instead of cycling.

Model deployable/operated units, entrypoints, actors, stores, and external systems
at a consistent level. Helpers and dependency names do not establish services.
Anchor every material element and relationship to `relative/path:line` evidence,
normally in triple-quoted descriptions. Trace important inbound/outbound flows;
include protocol, data class, trust crossing, and credential type when proven,
without copying secret values. Record uncertainty and justified omissions in
comments rather than inventing infrastructure.

Compare the persisted model with both reports so evidenced external interactions
are represented or explicitly omitted for an evidence-based reason. Validate once
more; a missing/failed CLI is not clean. Cumulative workspace export is automatic. Finish
successfully only with `valid: true` and durable `likec4/architecture` of media type
`text/vnd.likec4`. Keep the semantic result concise, with gaps and blockers when
present; omit DSL and storage revisions.
