Produce one `likec4/architecture` document (`text/vnd.likec4`) from the exact
`source`, `dependency_report`, and `project_report`, preserving the optional
exact `existing_likec4` seed. The Worker owns source access, retry recovery,
persisted `specification` -> `model` -> `views` phases, and DSL validation.

Keep work centered on this artifact with verifiable subtask outcomes. Do not
split exploration from construction or repeat completed work without a specific
unresolved requirement.

Acceptance: the model covers evidenced runtime units, actors, stores, inbound and
outbound flows, and relevant security/trust boundaries at a consistent level.
Material elements and relationships cite source. Compare with both reports and
explain omissions; avoid speculative infrastructure. Require a durable document
and clean final `validate_likec4` result. Return a concise semantic result with
gaps or blockers, without pasting DSL.
