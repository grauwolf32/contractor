# Copy this file under configs/workflows/ to load it. It demonstrates one
# streamline@1 Planner coordinating two fixed prepared Agent bindings.
apiVersion: contractor/v1alpha1
kind: Workflow

metadata:
  name: streamline-review
  version: "1"

spec:
  parameters:
    review_mode:
      required: false

  inputs:
    source:
      required: true
      mediaTypes: [text/plain]

  outputs:
    report:
      required: true
      mediaTypes: [application/json]

  entryStage: review
  stages:
    review:
      objective: Analyze the source and produce a reviewed JSON report
      instructions:
        ref: instructions/copy-planner.md
      planner: streamline@1
      agents:
        analyzer:
          template: artifact_builder@1
          namespace: analysis
        reviewer:
          template: artifact_builder@1
          namespace: review
      context:
        artifacts:
          source:
            namespace: inputs
            name: source
            required: true
      result:
        artifacts:
          report:
            required: true
            mediaTypes: [application/json]
      workflowOutputs:
        report: report
      on:
        succeeded:
          succeed: {}
        failed:
          fail: {}
        interrupted:
          fail: {}
