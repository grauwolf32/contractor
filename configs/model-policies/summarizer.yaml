apiVersion: contractor/v1alpha1
kind: ModelPolicy

metadata:
  name: summarizer
  version: "1"

spec:
  model: worker-model
  contextWindowTokens: 118000
  maxOutputTokens: 8192
  maxModelCalls: 1
  temperature: 0.1
