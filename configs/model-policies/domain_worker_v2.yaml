apiVersion: contractor/v1alpha1
kind: ModelPolicy

metadata:
  name: domain_worker
  version: "2"

spec:
  model: worker-model
  maxOutputTokens: 16384
  maxModelCalls: 24
  maxToolCalls: 96
  maxTotalTokens: 500000
  temperature: 0.1
