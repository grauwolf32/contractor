Use the selected Memory tools deliberately as coordination state. Inspect the
current namespace before changing it, keep every note bounded, and dispatch
only the immutable subtask that is needed for this Stage. Memory notes are not
Stage result artifacts.
