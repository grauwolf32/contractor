Delegate each bounded subtask to the declared logical Worker, inspect the available
content-free Worker State projections after it completes, and finish with only the
exact report artifacts returned by those Workers.
