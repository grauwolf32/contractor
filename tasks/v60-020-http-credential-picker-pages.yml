schema_version: '1.0'
id: V60-020
title: Make later Runtime credential pages selectable in HTTP target editor
milestone: project-review-v60
priority: P2
status: completed
depends_on:
- V60-001
languages:
- typescript
goal: Make later Runtime credential pages selectable in HTTP target editor.
context:
  authority: User requested splitting the independently reproduced review findings into tasks and beginning fixes
    on 2026-09-20.
  finding_ids:
  - CR-08
  design: docs/plans/2026-09-20-independent-review-fixes.md
  reproduction_report: docs/research/2026-09-20-independent-code-review.md
scope:
  in:
  - HTTP target credential picker follows pagination without silently dropping compatible credentials.
  - Selected credential remains visible; aggregate/page query identities are distinct and later-page errors are
    handled.
  out:
  - Unrelated large refactoring, production deployment or data mutation, live model calls.
acceptance_criteria:
- id: A1
  text: HTTP target credential picker follows pagination without silently dropping compatible credentials.
- id: A2
  text: Selected credential remains visible; aggregate/page query identities are distinct and later-page errors
    are handled.
tests:
- command: cd ui && corepack pnpm test --run src/routes/projects/http-target-editor.test.tsx
  covers:
  - A1
  - A2
- command: cd ui && corepack pnpm typecheck
  covers:
  - A1
  - A2
verification_requirements:
- Turn the discovery reproduction into a durable regression and verify the relevant failure before the fix.
- Record actual verification; PostgreSQL scenarios require an isolated disposable database and must not skip.
- Keep implementation scoped to this task, then record the full implementation commit and verification evidence
  separately.
implementation_commit: 18a48d44801481cddd15709f26df036da3b137d5
verification:
  evidence: tasks/evidence/v60-020.json
  results:
  - Four picker tests passed independently; picker plus Project journeys18 passed. Full UI suite451 passed; TypeScript/lint
    passed.
  - Distinct aggregate cache, later pages, error/retry, cursor cycle and retained current selection are covered.
