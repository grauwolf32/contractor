schema_version: '1.0'
id: V65-003
title: Expose Run recovery in API/UI and verify outage recovery across processes
milestone: run-admission-gateway-recovery
priority: P1
status: completed
implementation_commit: null
depends_on:
- V65-002
languages:
- go
- python
- typescript
- sql
- openapi
- markdown
goal: Expose Run recovery in API/UI and verify outage recovery across processes
context:
  authority: User explicitly requested tasks and implementation after the crAPI outage on 2026-09-20.
    Preserve other working-tree changes.
  design:
  - docs/plans/2026-09-20-run-gateway-recovery.md
scope:
  in:
  - Propagate pending/waiting through public API, generated clients, Run lists/details, queue, events,
    audit/eval polling and lifecycle operations.
  - Exercise three queued Runs against a gateway that becomes unavailable and recovers; preserve pending
    work and invocation identity without replaying completed tools.
  - Verify permanent request errors, cancellation, exhausted automatic recovery, single-probe arbitration
    and independent routes; log results in the Playground task.
  out:
  - Replay an entire Stage automatically after ambiguous tool effects.
  - Persist/restore ADK sessions after Runtime process loss.
  - Change crAPI truth or turn infrastructure failures into benchmark quality failures.
acceptance_criteria:
- id: A1
  then: Propagate pending/waiting through public API, generated clients, Run lists/details, queue, events,
    audit/eval polling and lifecycle operations.
- id: A2
  then: Exercise three queued Runs against a gateway that becomes unavailable and recovers; preserve pending
    work and invocation identity without replaying completed tools.
- id: A3
  then: Verify permanent request errors, cancellation, exhausted automatic recovery, single-probe arbitration
    and independent routes; log results in the Playground task.
tests:
  - command: "go test -tags=e2e ./tests/e2e -run ^TestGatewayRecovery.*AcrossProcesses$ -count=1 -timeout=10m"
    covers: [A1, A2, A3]
  - command: "cd ui && pnpm typecheck && pnpm exec vitest run src/api/runs.test.ts src/api/queue.test.ts src/routes/runs src/routes/queue.test.tsx src/routes/home.test.tsx"
    covers: [A1, A3]
  - command: "cd ui && pnpm build"
    covers: [A1]
verification_requirements:
  - PostgreSQL checks use CONTRACTOR_TEST_DATABASE_URL for an isolated test instance; process gates create and drop isolated schemas.
  - Evidence and remaining unrelated repository-wide failures are recorded in docs/plans/2026-09-20-run-gateway-recovery.md and the Playground task journal.
