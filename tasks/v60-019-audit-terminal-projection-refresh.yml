schema_version: '1.0'
id: V60-019
title: Refresh Audit projections at their terminal transition
milestone: project-review-v60
priority: P2
status: in_progress
depends_on:
- V60-001
languages:
- typescript
goal: Refresh Audit projections at their terminal transition.
context:
  authority: User requested splitting the independently reproduced review findings into tasks and beginning fixes
    on 2026-09-20.
  finding_ids:
  - CR-07
  design: docs/plans/2026-09-20-independent-review-fixes.md
  reproduction_report: docs/research/2026-09-20-independent-code-review.md
scope:
  in:
  - Coverage fetches final results when parent Audit changes revision/state within the same round.
  - Review related items/report polling and preserve explicit revision pins, pagination consistency and bounded
    query caches.
  out:
  - Unrelated large refactoring, production deployment or data mutation, live model calls.
acceptance_criteria:
- id: A1
  text: Coverage fetches final results when parent Audit changes revision/state within the same round.
- id: A2
  text: Review related items/report polling and preserve explicit revision pins, pagination consistency and bounded
    query caches.
tests:
- command: cd ui && corepack pnpm test --run src/routes/projects/audits
  covers:
  - A1
  - A2
- command: cd ui && corepack pnpm typecheck
  covers:
  - A1
  - A2
verification_requirements:
- Turn the discovery reproduction into a durable regression and verify the relevant failure before the fix.
- Record actual verification; PostgreSQL scenarios require an isolated disposable database and must not skip.
- Keep implementation scoped to this task, then record the full implementation commit and verification evidence
  separately.
