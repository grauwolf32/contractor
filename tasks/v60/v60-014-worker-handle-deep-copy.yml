schema_version: '1.0'
id: V60-014
title: Detach WorkerHandle AgentCard copies without shared mutation
milestone: project-review-v60
priority: P2
status: completed
depends_on:
- V60-001
languages:
- go
goal: Detach WorkerHandle AgentCard copies without shared mutation.
context:
  authority: User requested splitting the independently reproduced review findings into tasks and beginning fixes
    on 2026-09-20.
  finding_ids:
  - CR-02
  design: docs/plans/2026-09-20-independent-review-fixes.md
  reproduction_report: docs/research/2026-09-20-independent-code-review.md
scope:
  in:
  - AgentCard clones share no mutable maps/slices, including nested data; the original is never mutated.
  - Parallel clone/read operations pass race detection and unsupported copy values are handled explicitly.
  out:
  - Unrelated large refactoring, production deployment or data mutation, live model calls.
acceptance_criteria:
- id: A1
  text: AgentCard clones share no mutable maps/slices, including nested data; the original is never mutated.
- id: A2
  text: Parallel clone/read operations pass race detection and unsupported copy values are handled explicitly.
tests:
- command: go test -race -count=1 ./internal/planner/...
  covers:
  - A1
  - A2
verification_requirements:
- Turn the discovery reproduction into a durable regression and verify the relevant failure before the fix.
- Record actual verification; PostgreSQL scenarios require an isolated disposable database and must not skip.
- Keep implementation scoped to this task, then record the full implementation commit and verification evidence
  separately.
implementation_commit: bcdfd661c15bf60a7f7e017a828e285021b4bde1
verification:
  evidence: tasks/evidence/v60-014.json
  results:
  - Six planner packages passed with race detector; database-dependent unrelated cases skipped without a DSN.
  - Clone marshals into a nil destination map and omits invalid non-JSON cards; nested copies are independent.
