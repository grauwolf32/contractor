schema_version: '1.0'
id: V60-015
title: Bind placement and RuntimeConfig rebind credential reads to their transaction
milestone: project-review-v60
priority: P2
status: completed
depends_on:
- V60-001
languages:
- go
goal: Bind placement and RuntimeConfig rebind credential reads to their transaction.
context:
  authority: User requested splitting the independently reproduced review findings into tasks and beginning fixes
    on 2026-09-20.
  finding_ids:
  - CR-03
  design: docs/plans/2026-09-20-independent-review-fixes.md
  reproduction_report: docs/research/2026-09-20-independent-code-review.md
scope:
  in:
  - Managed LLM and Runtime credential reads inside placement use the owning transaction without reacquiring the
    pool.
  - RuntimeConfig Rebind retains credential lifecycle fencing and validates through its transaction.
  - Real PostgreSQL minimal-pool/saturated-pool tests and existing credential lifecycle tests pass.
  out:
  - Unrelated large refactoring, production deployment or data mutation, live model calls.
acceptance_criteria:
- id: A1
  text: Managed LLM and Runtime credential reads inside placement use the owning transaction without reacquiring
    the pool.
- id: A2
  text: RuntimeConfig Rebind retains credential lifecycle fencing and validates through its transaction.
- id: A3
  text: Real PostgreSQL minimal-pool/saturated-pool tests and existing credential lifecycle tests pass.
tests:
- command: go test -race -count=1 ./internal/controlplane ./internal/runtimeconfig ./internal/credentials ./internal/app
  covers:
  - A1
  - A2
  - A3
verification_requirements:
- Turn the discovery reproduction into a durable regression and verify the relevant failure before the fix.
- Record actual verification; PostgreSQL scenarios require an isolated disposable database and must not skip.
- Keep implementation scoped to this task, then record the full implementation commit and verification evidence
  separately.
implementation_commit: 6cebf5a0273439158eb4eba01d5b719347c44522
verification:
  evidence: tasks/evidence/v60-015.json
  results:
  - 'All four packages passed with race detector and disposable PostgreSQL: controlplane,runtimeconfig,credentials,app.'
  - Six pre-fix cases failed with one available pool connection; corrected tests cover managed/runtime placement,
    Rebind, management create/rebind/replay, pins and retained credential lifecycle barriers.
