schema_version: '1.0'
id: V65-001
title: Classify gateway failures consistently and recognize model unloads
milestone: run-admission-gateway-recovery
priority: P1
status: completed
implementation_commit: null
depends_on: []
languages:
- go
- python
- typescript
- sql
- openapi
- markdown
goal: Classify gateway failures consistently and recognize model unloads
context:
  authority: User explicitly requested tasks and implementation after the crAPI outage on 2026-09-20.
    Preserve other working-tree changes.
  design:
  - docs/plans/2026-09-20-run-gateway-recovery.md
scope:
  in:
  - Recognize the observed LM Studio model-unloaded HTTP 400, including its LiteLLM wrapper, as transient
    model unavailability; arbitrary 400 remains permanent.
  - Retain only safe normalized error code/status; use one retryability decision in transport, model adapter,
    Worker and telemetry.
  - Cover transport failures, Retry-After, authentication/quota/context errors, cancellation and response-data
    redaction.
  out:
  - Replay an entire Stage automatically after ambiguous tool effects.
  - Persist/restore ADK sessions after Runtime process loss.
  - Change crAPI truth or turn infrastructure failures into benchmark quality failures.
acceptance_criteria:
- id: A1
  then: Recognize the observed LM Studio model-unloaded HTTP 400, including its LiteLLM wrapper, as transient
    model unavailability; arbitrary 400 remains permanent.
- id: A2
  then: Retain only safe normalized error code/status; use one retryability decision in transport, model
    adapter, Worker and telemetry.
- id: A3
  then: Cover transport failures, Retry-After, authentication/quota/context errors, cancellation and response-data
    redaction.
tests:
  - command: "go test ./internal/gatewayrecovery"
    covers: [A1, A2, A3]
  - command: "cd runtime && uv run pytest tests/test_gateway_error_classification.py tests/test_gateway_client.py tests/test_openai_gateway_llm.py tests/test_metrics.py tests/test_gateway_recovery.py"
    covers: [A1, A2, A3]
verification_requirements:
  - PostgreSQL checks use CONTRACTOR_TEST_DATABASE_URL for an isolated test instance; process gates create and drop isolated schemas.
  - Evidence and remaining unrelated repository-wide failures are recorded in docs/plans/2026-09-20-run-gateway-recovery.md and the Playground task journal.
