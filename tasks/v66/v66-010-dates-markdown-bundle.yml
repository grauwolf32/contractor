schema_version: '1.0'
id: V66-010
title: Relative dates, scaled Markdown headings and smaller main bundle
milestone: ui-design-review-2
priority: P2
status: completed
implementation_commit: 9e5d19dcdd3f2662135eea3dd28dfb428db07a98
depends_on: []
languages:
- typescript
- css
- markdown
goal: Relative dates, scaled Markdown headings and smaller main bundle.
context:
  authority: User approved the 2026-09-22 follow-up design review and asked to record and start the task
    list.
  design: docs/plans/2026-09-22-ui-design-review-2.md
  review: docs/research/2026-09-22-ui-design-review-2.md
  finding_ids:
  - E10
  baseline: .local audit capture of 2026-09-21 (92 states at 1440 and 25 at 390) and the deployed demo
    stand at UI 0.5.3.
scope:
  in:
  - Use RecordedTime (relative with exact tooltip) for list and card timestamps; keep absolute timestamps
    in detail metadata.
  - Scale headings inside the shared Markdown preview so document headings never exceed the page heading.
  - Split react-markdown and remark-gfm out of the main chunk via lazy routes; record chunk sizes before
    and after.
  out:
  - Changing the LikeC4 or Scalar previews.
acceptance_criteria:
- id: A1
  text: Use RecordedTime (relative with exact tooltip) for list and card timestamps; keep absolute timestamps
    in detail metadata.
- id: A2
  text: Scale headings inside the shared Markdown preview so document headings never exceed the page heading.
- id: A3
  text: Split react-markdown and remark-gfm out of the main chunk via lazy routes; record chunk sizes
    before and after.
tests:
- command: cd ui && corepack pnpm test --run && corepack pnpm build
  covers:
  - A1
  - A2
  - A3
verification_requirements:
- Run the listed tests, eslint, prettier and tsc; visual changes are checked in the browser against the
  populated demo data.
- Record before/after evidence in the task file under verification.
verification:
  focused_tests: vitest 78 files / 615 tests pass twice in a row; test:server 11/11; eslint, prettier, tsc (app and e2e) clean; vite build ok.
  bundle: eager index 1,194,209 B -> index 72 KB + vendor 314 KB; lazy chunks (likec4, Scalar, openapi) unchanged.
  dates: RecordedTime in queue, runs list, run artifacts, artifacts, allocations, credentials, runtime configs, home, skills; formatTimestamp fixed to en-US medium format for detail blocks.
  leftovers: evals/pair.tsx and settings/git-key.tsx still call toLocaleString; .result-summary keeps 0.95rem headings by design.
