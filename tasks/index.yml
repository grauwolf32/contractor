schema_version: "1.0"

project:
  name: contractor-v2
  objective: >-
    Deliver a single-VM workflow runtime whose Control Plane and public server
    are implemented in Go and whose single-slot Runtime Agent is implemented in
    Python.

execution_policy:
  task_files_are_self_contained: true
  dependency_rule: >-
    A task may start only after every ID in depends_on is complete. The worker
    may inspect code produced by those dependencies but is not expected to read
    docs/spec or know earlier design discussions.
  change_rule: >-
    Keep each implementation task in its own commit unless two tasks must be
    landed atomically to keep the repository buildable.
  completion_rule: >-
    A task is complete only when every acceptance criterion passes and all
    commands listed under tests succeed.
  implementation_commit_rule: >-
    Every completed task file records implementation_commit as the full hash of
    the last commit whose primary purpose completed that task. Later metadata or
    unrelated maintenance commits do not replace it.
  priorities:
    P0: Required for the first runnable MVP.
    P1: Required to harden the MVP into the specified first slice.

fixed_decisions:
  server:
    language: Go 1.25
    module: github.com/grauwolf32/contractor
    http_stack: Go standard library net/http
    database_driver: github.com/jackc/pgx/v5
  runtime_agent:
    language: Python 3.13
    packaging: uv with a src-layout package
    agent_framework: Google ADK Python
    a2a_sdk: Official a2a-sdk Python package
  planner:
    mvp: >-
      passthrough@1 is deterministic Go code and makes one A2A call. It records
      a durable Contractor planner session but does not need an LLM or ADK event
      loop.
    first_slice: >-
      streamline@1 and router@1 use Google ADK Go with a PostgreSQL-backed
      SessionService and a bounded typed subtask plan. Streamline has exactly
      one logical Worker; Router selects among a fixed non-empty logical Worker
      mapping without observing physical Runtime Agent identities.
  a2a:
    protocol: A2A 1.0
    binding: HTTP JSON-RPC
    go_sdk: github.com/a2aproject/a2a-go/v2
  persistence:
    database: PostgreSQL
    mvp_blob_backend: PostgreSQL bytea addressed through ArtifactStore
  deployment:
    topology: >-
      One VM, one Server process, PostgreSQL, and one or more single-slot
      Runtime Agent processes whose immutable environments may expose different
      positive capability snapshots.
    private_security: Deployment-CA mTLS
    public_auth: >-
      One owner-only local-auth principal backs bounded in-memory browser
      sessions and the retained bearer-token automation path; cookie mutations
      require exact-origin CORS and session-bound CSRF.

milestones:
  - id: mvp
    exit_task: MVP-015
    outcome: >-
      A local user uploads one artifact, starts one exact Workflow version, and
      receives one frozen output after a Go Scheduler invokes one Python ADK
      Worker over A2A. All private traffic uses mTLS.
  - id: first-slice-hardening
    exit_task: V1-006
    outcome: >-
      Cancellation, lease fencing, retry, metrics, race safety, and the first
      model-backed Streamline Planner satisfy the v2 first-slice contracts.
  - id: project-workflows-mvp
    exit_task: V2-007
    outcome: >-
      A user can upload a bounded ZIP source tree and run the four-Stage
      OpenAPI or LikeC4 workflow through the real Go Server and Python Runtime
      Agent, receiving validated frozen documents and validation reports.
  - id: project-workflows-quality
    exit_task: V2-009
    outcome: >-
      The port has an opt-in live-model quality gate and explicit workflow
      variants that reuse caller-supplied discovery reports without adding
      implicit cross-Run cache semantics to Scheduler.
  - id: project-workflows-operations
    exit_task: V2-010
    outcome: >-
      Every Worker tool loop has explicit cumulative model, tool, and token
      budgets and terminates with complete safe metrics before Stage retry.
  - id: planner-routing-and-escalation
    exit_task: V3-010
    outcome: >-
      Streamline and Router execute a durable bounded subtask plan through
      deterministic Worker adapters, while Scheduler owns retry and pinned
      execution-configuration escalation.
  - id: server-ui-and-operations
    exit_task: V4-011
    outcome: >-
      A separately deployed React UI uses the committed public API, local
      browser sessions and a resumable WebSocket to operate Runs, inspect live
      Planner progress and manage active LLM configuration and credentials.
  - id: live-model-reliability
    exit_task: V5-003
    outcome: >-
      Tool-complete Workers recover malformed result envelopes through bounded
      structured output, project-scale LikeC4 variants use explicit larger
      finite budgets, and owners can inspect safe actionable attempt errors.
  - id: lifecycle-review-hardening
    exit_task: V6-003
    outcome: >-
      Artifact revocation and concurrent writes are linearizable, terminal
      allocation cleanup is bounded and cannot block unrelated Runs, exact Run
      replay survives mutable dependency deletion, and volatile registry
      history remains bounded and operationally current.
  - id: runtime-capability-placement
    exit_task: V7-004
    outcome: >-
      Runtime Agents probe and freeze environment-specific capabilities before
      registration, Control Plane finds complete compatible Stage assignments,
      Operations explains each positive snapshot, and a real heterogeneous
      process test proves capacity waiting plus later execution.

current_checkpoint:
  completed_through: V7-003
  next_task: V7-004
  summary: >-
    Runtime Agents now probe and freeze their usable runtime, sandbox, and
    per-tool capabilities before registration. Control Plane now finds a full
    deterministic capability-compatible assignment, and Operations API/UI
    exposes each accepted positive snapshot without probe diagnostics. Next,
    prove capacity waiting and later execution across heterogeneous real
    Runtime Agent processes.

current_planning_review:
  reviewed_on: "2026-08-31"
  verdict: ready_to_implement
  checks:
    - >-
      V7-001 changes only Python discovery, frozen registration construction,
      and Runtime-side AllocationSpec enforcement; the existing registration
      wire shape remains sufficient.
    - >-
      V7-002 is an independent Go allocator correction. Its complete matching
      requirement is stronger than first-fit but does not introduce scoring,
      affinity, partial reservation, or new Workflow semantics.
    - >-
      V7-003 projects the already accepted positive snapshot through the
      existing Operations API/UI; it neither sends negative probe diagnostics
      nor adds a capability mutation endpoint.
    - >-
      V7-004 is last because its real-process scenario depends on discovery,
      matching, and visibility. It proves that compatible-capacity waiting
      stays inside one Stage attempt before a later agent joins.
    - >-
      The dependency graph permits V7-001 and V7-002 to be implemented
      independently, then converges through V7-003 and V7-004 without a
      circular dependency.
    - >-
      Every task is self-contained for a worker without spec context, has one
      primary implementation boundary, explicit failure cases, acceptance
      criteria, test commands, and inherits the full implementation-commit rule.
  accepted_risks:
    - >-
      Capability dimensions remain composable sets in v1alpha1. An environment
      with combination-specific incompatibilities must advertise a common safe
      subset or run separate process configurations.
    - >-
      Current-fleet absence and temporarily busy compatible capacity share one
      ErrInsufficientCapacity path until the Stage deadline or cancellation;
      proactive unschedulable diagnosis and autoscaling remain deferred.
    - >-
      Probe failures are local redacted diagnostics. Operations deliberately
      shows only positive capabilities, not paths, command output, environment
      values, or a remotely actionable re-probe control.

project_workflow_port:
  source_repository: /home/ruslan/src/contractor-old
  reviewed_revision: 9c76b56cf7b83377fb1dd5e4a17440fa27b723f3
  source_workflows:
    - contractor/workflows/oas_building
    - contractor/workflows/likec4_building
  retained_shape: >-
    dependency discovery, project discovery, document build, and repair-only
    validation remain four serial Stage executions using passthrough@1.
  intentional_differences:
    - >-
      Source code is a required application/zip Workflow input forked to
      inputs/source; Runtime never receives an arbitrary host project path.
    - >-
      Discovery reports and generated documents are explicit RunScope
      artifacts. A later Stage never reads an earlier Worker's sandbox.
    - >-
      Existing OpenAPI and LikeC4 documents are optional Workflow inputs and
      are copied into the document Agent's Namespace before mutation.
    - >-
      The default workflows rerun discovery. The old implicit cross-Run
      artifact-exists skip is represented later by explicit precomputed-input
      workflow variants, not hidden Scheduler state.
    - >-
      Old TaskRunner iterations/max_steps are not copied literally. One v2
      Stage is one bounded Planner/Worker invocation; whole-Stage retry remains
      explicit in Workflow transitions and the deployment Planner deadline is
      the hard wall bound.

planning_review:
  reviewed_on: "2026-08-30"
  verdict: project_workflow_increment_complete
  checks:
    - >-
      Dependency graph is acyclic and reaches a deterministic P0 exit through
      V2-001 -> V2-002 -> V2-003/V2-005 -> V2-004/V2-006 -> V2-007.
    - >-
      Every P0 task is independently testable, keeps one primary responsibility,
      and names its exact code/config/test deliverables and model-visible tools.
    - >-
      Server and Runtime descriptor parity is covered before a Workflow can
      select a Toolset; domain state remains RunScope artifacts rather than
      allocation-local hidden persistence.
    - >-
      The deterministic end-to-end task crosses all production boundaries and
      substitutes only the external model and fixed validator executables.
    - >-
      V2-008 exercises the same production stack against a real
      OpenAI-compatible model and records either semantic success or a bounded,
      actionable diagnostic without making ordinary CI network-dependent.
    - >-
      V2-009 expresses discovery reuse as explicit versioned Workflow inputs;
      it adds no hidden cache lookup or conditional Scheduler behavior.
    - >-
      V2-010 closes the live-evaluation runaway-loop finding at the Worker
      boundary and proves bounded failure, complete metrics, release, and slot
      reuse end to end.
  accepted_risks:
    - >-
      application/zip remains subject to the existing 16 MiB Artifact limit;
      larger repositories require the deferred streaming/S3 increment.
    - >-
      Vacuum and LikeC4 are direct Runtime environment prerequisites. The
      planned startup probes omit their validation tools when unavailable, so
      a requiring Stage waits for compatible capacity rather than assigning a
      Worker that cannot honor the validator contract.
    - >-
      Initial source search is bounded text search rather than the legacy
      tree-sitter/graph index. Live evidence shows that it is adequate for the
      compact fixture; larger or polyglot repositories may justify a future
      source-analysis@2.
    - >-
      Explicit caller-selected discovery reuse is implemented. Automatic cache
      keys, provenance compatibility checks, and invalidation remain deferred.
    - >-
      Model quality and result-envelope reliability remain provider-dependent.
      Finite Worker budgets now bound time and spend, but policy tuning across
      larger repositories needs a broader future evaluation matrix.

tasks:
  - {id: MVP-001, file: mvp-001-bootstrap-monorepo.yml}
  - {id: MVP-002, file: mvp-002-wire-contracts.yml}
  - {id: MVP-003, file: mvp-003-config-loader.yml}
  - {id: MVP-004, file: mvp-004-postgres-state.yml}
  - {id: MVP-005, file: mvp-005-artifact-store.yml}
  - {id: MVP-006, file: mvp-006-public-run-api.yml}
  - {id: MVP-007, file: mvp-007-local-pki-mtls.yml}
  - {id: MVP-008, file: mvp-008-control-plane-registry.yml}
  - {id: MVP-009, file: mvp-009-runtime-control-client.yml}
  - {id: MVP-010, file: mvp-010-allocation-worker-factory.yml}
  - {id: MVP-011, file: mvp-011-private-artifact-api.yml}
  - {id: MVP-012, file: mvp-012-python-adk-a2a.yml}
  - {id: MVP-013, file: mvp-013-go-passthrough-planner.yml}
  - {id: MVP-014, file: mvp-014-workflow-scheduler.yml}
  - {id: MVP-015, file: mvp-015-end-to-end.yml}
  - {id: V1-001, file: v1-001-cancellation-abort.yml}
  - {id: V1-002, file: v1-002-lease-fencing.yml}
  - {id: V1-003, file: v1-003-retry-multistage.yml}
  - {id: V1-004, file: v1-004-metrics.yml}
  - {id: V1-005, file: v1-005-races-security.yml}
  - {id: V1-006, file: v1-006-streamline-planner.yml}
  - {id: V2-001, file: v2-001-text-artifact-toolset.yml}
  - {id: V2-002, file: v2-002-source-analysis-toolset.yml}
  - {id: V2-003, file: v2-003-openapi-toolset.yml}
  - {id: V2-004, file: v2-004-openapi-workflow.yml}
  - {id: V2-005, file: v2-005-likec4-toolset.yml}
  - {id: V2-006, file: v2-006-likec4-workflow.yml}
  - {id: V2-007, file: v2-007-project-workflows-e2e.yml}
  - {id: V2-008, file: v2-008-project-workflows-live-eval.yml}
  - {id: V2-009, file: v2-009-precomputed-analysis-variants.yml}
  - {id: V2-010, file: v2-010-worker-invocation-budgets.yml}
  - {id: V3-001, file: v3-001-planner-finish-contract.yml}
  - {id: V3-002, file: v3-002-typed-planner-plan.yml}
  - {id: V3-003, file: v3-003-single-worker-streamline.yml}
  - {id: V3-004, file: v3-004-router-planner.yml}
  - {id: V3-005, file: v3-005-llm-gateway-config.yml}
  - {id: V3-006, file: v3-006-resolved-execution-config.yml}
  - {id: V3-007, file: v3-007-escalation-config-profiles.yml}
  - {id: V3-008, file: v3-008-scheduler-escalation.yml}
  - {id: V3-009, file: v3-009-planner-plan-events.yml}
  - {id: V3-010, file: v3-010-routing-escalation-e2e.yml}
  - {id: V4-001, file: v4-001-public-openapi-contract.yml}
  - {id: V4-001A, file: v4-001a-public-query-api.yml}
  - {id: V4-002, file: v4-002-managed-config-publication.yml}
  - {id: V4-003, file: v4-003-encrypted-credential-store.yml}
  - {id: V4-003A, file: v4-003a-credential-lifecycle.yml}
  - {id: V4-004, file: v4-004-litellm-key-manager.yml}
  - {id: V4-005, file: v4-005-operations-api.yml}
  - {id: V4-006, file: v4-006-browser-session-security.yml}
  - {id: V4-007, file: v4-007-run-event-websocket.yml}
  - {id: V4-007A, file: v4-007a-public-api-version.yml}
  - {id: V4-008, file: v4-008-react-ui-foundation.yml}
  - {id: V4-009, file: v4-009-workflow-run-ui.yml}
  - {id: V4-009A, file: v4-009a-workflow-run-creation-ui.yml}
  - {id: V4-009B, file: v4-009b-live-run-ui.yml}
  - {id: V4-010, file: v4-010-operations-ui.yml}
  - {id: V4-011, file: v4-011-browser-e2e.yml}
  - {id: V5-001, file: v5-001-structured-worker-result-recovery.yml}
  - {id: V5-002, file: v5-002-high-budget-likec4-streamline.yml}
  - {id: V5-003, file: v5-003-attempt-diagnostics.yml}
  - {id: V6-001, file: v6-001-artifact-lifecycle-race-safety.yml}
  - {id: V6-002, file: v6-002-terminal-release-recovery.yml}
  - {id: V6-003, file: v6-003-idempotency-and-registry-retention.yml}
  - {id: V7-001, file: v7-001-runtime-capability-probes.yml}
  - {id: V7-002, file: v7-002-complete-capability-placement.yml}
  - {id: V7-003, file: v7-003-operations-capability-visibility.yml}
  - {id: V7-004, file: v7-004-heterogeneous-capability-e2e.yml}
