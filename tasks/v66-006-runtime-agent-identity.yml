schema_version: '1.0'
id: V66-006
title: Name Runtime Agents and de-emphasise offline identities
milestone: ui-design-review-2
priority: P2
status: completed
implementation_commit: 2f87023df4a38920e7a0bb11aa973089ffa087e2
depends_on: []
languages:
- typescript
- css
- markdown
goal: Name Runtime Agents and de-emphasise offline identities.
context:
  authority: User approved the 2026-09-22 follow-up design review and asked to record and start the task
    list.
  design: docs/plans/2026-09-22-ui-design-review-2.md
  review: docs/research/2026-09-22-ui-design-review-2.md
  finding_ids:
  - E06
  baseline: .local audit capture of 2026-09-21 (92 states at 1440 and 25 at 390) and the deployed demo
    stand at UI 0.5.3.
scope:
  in:
  - Show hostname or advertised name from agent metadata as the card title with the short id as secondary
    text.
  - Render offline identities in a collapsed muted group below online agents and replace empty observation
    cells with one line.
  - Add a forget action for offline identities only if the API supports it; otherwise state retention
    in one line.
  out:
  - Runtime registration or heartbeat changes.
acceptance_criteria:
- id: A1
  text: Show hostname or advertised name from agent metadata as the card title with the short id as secondary
    text.
- id: A2
  text: Render offline identities in a collapsed muted group below online agents and replace empty observation
    cells with one line.
- id: A3
  text: Add a forget action for offline identities only if the API supports it; otherwise state retention
    in one line.
tests:
- command: cd ui && corepack pnpm test --run src/routes/operations/runtime-agents
  covers:
  - A1
  - A2
  - A3
verification_requirements:
- Run the listed tests, eslint, prettier and tsc; visual changes are checked in the browser against the
  populated demo data.
- Record before/after evidence in the task file under verification.
verification:
  focused_tests: vitest src/routes/operations 6 files / 33 tests pass; eslint, prettier, tsc (app and e2e) clean.
  naming: first saved label, else live instanceId, else Agent plus short id; short id and copy control remain.
  forget: DELETE /v1/operations/runtime-agent-principals/{id} with If-Match revision; offered only for offline identities without saved labels (server returns 409 otherwise).
  browser: not checked against demo data; owned by V66-011.
