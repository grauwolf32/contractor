schema_version: '1.0'
id: V60-013
title: Retain Runtime operation ownership until cancellation cleanup completes
milestone: project-review-v60
priority: P2
status: in_progress
depends_on:
- V60-001
languages:
- python
goal: Retain Runtime operation ownership until cancellation cleanup completes.
context:
  authority: User requested splitting the independently reproduced review findings into tasks and beginning fixes
    on 2026-09-20.
  finding_ids:
  - CR-01
  design: docs/plans/2026-09-20-independent-review-fixes.md
  reproduction_report: docs/research/2026-09-20-independent-code-review.md
scope:
  in:
  - Source archive extraction, staging install and cleanup remain owned until threads finish.
  - LikeC4 and OpenAPI validator cancellation stops and reaps subprocesses before tool/allocation release.
  - Deterministic cancellation regressions cover allocation release, no recreated workspace, process termination
    and normal operation.
  out:
  - Unrelated large refactoring, production deployment or data mutation, live model calls.
acceptance_criteria:
- id: A1
  text: Source archive extraction, staging install and cleanup remain owned until threads finish.
- id: A2
  text: LikeC4 and OpenAPI validator cancellation stops and reaps subprocesses before tool/allocation release.
- id: A3
  text: Deterministic cancellation regressions cover allocation release, no recreated workspace, process termination
    and normal operation.
tests:
- command: cd runtime && .venv/bin/python -m pytest -W error tests/test_source_analysis_toolset.py tests/test_likec4_toolset.py
    tests/test_openapi_toolset.py tests/test_allocation.py tests/test_abort.py tests/test_projectfs_operation_guard.py
    tests/test_tool_operation_ownership.py tests/test_tool_process.py tests/test_scan_toolset.py tests/test_scan_ffuf.py
    tests/test_http_proxy_adapter.py tests/test_local_direct_tool_consumers.py
  covers:
  - A1
  - A2
  - A3
verification_requirements:
- Turn the discovery reproduction into a durable regression and verify the relevant failure before the fix.
- Record actual verification; PostgreSQL scenarios require an isolated disposable database and must not skip.
- Keep implementation scoped to this task, then record the full implementation commit and verification evidence
  separately.
