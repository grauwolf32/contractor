schema_version: '1.0'
id: V66-005
title: Gather execution configuration under one Operations section
milestone: ui-design-review-2
priority: P2
status: in_progress
implementation_commit: null
depends_on: []
languages:
- typescript
- css
- markdown
goal: Gather execution configuration under one Operations section.
context:
  authority: User approved the 2026-09-22 follow-up design review and asked to record and start the task
    list.
  design: docs/plans/2026-09-22-ui-design-review-2.md
  review: docs/research/2026-09-22-ui-design-review-2.md
  finding_ids:
  - E05
  baseline: .local audit capture of 2026-09-21 (92 states at 1440 and 25 at 390) and the deployed demo
    stand at UI 0.5.3.
scope:
  in:
  - Move Runs → Configuration (RuntimeConfig versions, bindings, publish) under Operations as a Configuration
    section next to LLM configurations and Credentials; keep old route as a redirect.
  - Add scope labels (server-wide, personal, project) to each configuration group and contextual links
    from Project Settings and Run detail.
  - Update navigation tests and browser journeys that open runtime configuration.
  out:
  - Changing configuration contracts or publish semantics.
acceptance_criteria:
- id: A1
  text: Move Runs → Configuration (RuntimeConfig versions, bindings, publish) under Operations as a Configuration
    section next to LLM configurations and Credentials; keep old route as a redirect.
- id: A2
  text: Add scope labels (server-wide, personal, project) to each configuration group and contextual links
    from Project Settings and Run detail.
- id: A3
  text: Update navigation tests and browser journeys that open runtime configuration.
tests:
- command: cd ui && corepack pnpm test --run src/routes/operations src/routes/runs
  covers:
  - A1
  - A2
  - A3
verification_requirements:
- Run the listed tests, eslint, prettier and tsc; visual changes are checked in the browser against the
  populated demo data.
- Record before/after evidence in the task file under verification.
